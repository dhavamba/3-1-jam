using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManageUI : MonoBehaviour
{
    /*
    private StateMenu state;

    public StateMenu State
    {
        set
        {
            state = value;
        }
    }

    public void ChangeState(string st)
    {
        State = (StateMenu)System.Enum.Parse(typeof(StateMenu), st);
    }

    */
}
